#!/usr/bin/env bash
# Manual pre-commit / CI stage for OpenTelemetry export and redaction.
# Fails closed until the telemetry package lands a real proof.
set -euo pipefail
echo "OTel smoke is not implemented yet; refusing empty pass." >&2
exit 2
