apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "science-platform.fullname" . }}-test-connection"
  labels:
    {{- include "science-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "science-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
